import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Navbar } from "@/components/navbar"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Kathmandu Orientation Company - Professional Training Provider",
  description:
    "Leading training provider in Nepal offering pre-departure orientation, language classes, IT courses, and professional development programs for blue-collar and white-collar careers.",
  keywords:
    "training, orientation, Nepal, Japanese language, English language, IT courses, pre-departure, professional development",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
